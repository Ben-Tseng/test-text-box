Array.from(document.querySelectorAll('button'))
    .find(btn => btn.textContent.toLowerCase().includes('resolve without'))?.click();
setTimeout(() => {
            document.getElementById("outcome-wizard-submit-btn").click();
        }, 2000);     }}
